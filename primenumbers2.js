let numbers
let checker
let a=false
for(numbers=2; numbers<=10; numbers++)    {

    for(checker=1 ; checker<=10; checker++) {
       // console.log(numbers,checker,numbers%checker)
        if(numbers%checker==0)
        {
            
            a=false    
        }
        else
        {
            console.log(numbers,checker)
            a=true
        }
    }
    if(a==true)
    {
        }
    
    





}

/**
 
 */